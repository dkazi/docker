services:
  logguard:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - logdata:/app
    restart: unless-stopped
    environment:
      - BACKEND_URL=http://localhost:5000

volumes:
  logdata:
